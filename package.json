{
  "name": "webframework",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start": "concurrently \"npm --prefix apps/back start\" \"npm --prefix apps/front start\"",
    "build": "npm --prefix apps/front run build",
    "lint": "npm --prefix apps/front run lint"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.0.0",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "eslint": "^9",
    "eslint-config-next": "^16.1.1"
  }
}
